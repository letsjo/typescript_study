/**
 * 유닛(`unit`) 타입 또는 리터럴(`literal`) 타입은 작은 집합으로 한 가지 값만 포함하는 타입입니다.
 * `const` 로 선언된 값은 값 변경이 불가하기 때문에 값 그대로 타입으로 적용된다.
 */

type A = 'A';
type B = 'B';
type Twelve = 12;

const Third = 3;

export default {};
